# Dataset Folder

Place the LIAR dataset files here:

- `train.tsv`
- `valid.tsv`
- `test.tsv`
- `README`

The training command expects these files by default. This project intentionally does not commit dataset files or generated model artifacts.

{
  "name": "truth-likelihood-analyzer",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered Truth Likelihood Analyzer using MERN Stack and Python NLP.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
